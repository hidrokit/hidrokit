# Site setting
title: hidrokit
description: analisis hidrologi dengan python

# Remote Setting
baseurl: "/hidrokit"
url: "https://hidrokit.github.io"

# Theme
# theme: "just-the-docs"
# remote_theme: pmarsceill/just-the-docs@v0.2.5
remote_theme: just-the-docs/just-the-docs

# Theme settings
# Color scheme currently only supports "dark" or nil (default)
color_scheme: nil

# Enable or disable the site search
# Use this command to update index: bundle exec just-the-docs rake search:init
search_enabled: true

# Aux links for the upper right navigation
aux_links:
  "View on GitHub":
    - "//github.com/hidrokit/hidrokit"
